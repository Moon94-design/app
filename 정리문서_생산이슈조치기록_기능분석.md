# 생산/이슈/조치 기록 기능 정리 문서

## 1. 페이지별 기능 목록 (체크리스트)

### 1.1 Production (생산기록)
**파일**: [company-docs/src/app/pages/register/RegisterProductionDaily.tsx](company-docs/src/app/pages/register/RegisterProductionDaily.tsx)

#### 입력 필드
- [x] 기록일 (recordDate)
- [x] 작성자 이름 (writerName)
- [x] 작성자 직책 (writerRole)
- [x] 지부 (site: 대구/성주)
- [x] 제목 (title)
- [x] 상세 내용 (details - textarea)
- [x] 태그 (tagsText - 쉼표 구분)
- [x] 생산 라인 목록 (lines)
  - 교대 (shift: 주간/오후/야간)
  - 제품 (product: 분쇄품/펠렛)
  - 품목 (item: PP/PE)
  - 포대수 (bags), 중량 (kg), 메모 (memo)

#### 버튼
- [x] 생산 라인 추가
- [x] 생산 라인 삭제
- [x] 이슈 추가 (패널 토글)
- [x] 저장 (upsertSave)
- [x] 초기화
- [x] 기본값으로 채우기

#### 자동 기능
- [x] 제목 자동 생성 (buildAutoTitle)
  - 형식: `{recordDate} {writerName} {writerRole} 생산일지`
  - 포커스/변경 시 자동 업데이트
- [x] 태그 추천 (typingSuggestions)
  - 실시간 타이핑 기반 (2글자 이상)
  - prefix/contains/suffix 매칭
  - 3-gram 윈도우 보조
  - 점수 기반 정렬 (매칭 강도 → 태그 길이)
  - 더보기 (30개 → 전체)
  - 선택 태그 자동 커밋
- [x] 태그 색상 자동 구분
  - 시스템 태그 (기준정보): accent 색상
  - 개인 태그: 기본 색상

#### 저장 방식 (확정)
- **저장소**: localStorage → `daily_production_v1`
- **구조**: 문서 ID 기반 upsert
  - ID: `PROD_{recordDate}_{site}_{writerName}`
  - 1개 문서 = 1일/1지부/1작성자
- **Draft**: localStorage → `draft_production_daily_schema_v2`
- **API**: [company-docs/src/data/localRepo.ts](company-docs/src/data/localRepo.ts) `productionDaily()`

#### 목록 표시
- [x] 하단에 저장된 문서 목록 표시
- [x] 삭제 버튼 제공
- [x] 미리보기: 제목, 날짜, 라인 개수

#### 삭제/수정 정책
- 문서 단위 삭제 가능
- 기존 문서 수정 시 upsert로 덮어쓰기

---

### 1.2 Issue (이슈기록)
**페이지**: [company-docs/src/app/pages/register/RegisterIssue.tsx](company-docs/src/app/pages/register/RegisterIssue.tsx)  
**공통 폼**: [company-docs/src/base/components/form/IssueForm.tsx](company-docs/src/base/components/form/IssueForm.tsx)

#### 입력 필드
- [x] 기록일 (recordDate)
- [x] 작성자 이름 (writerName)
- [x] 작성자 직책 (writerRole)
- [x] 지부 (site: 선택)
- [x] 카테고리 (category: 품질/설비/안전)
- [x] 제목 (title - 자동 생성)
- [x] 상세 내용 (details)
- [x] 태그 (tagsText)

#### 카테고리별 입력 필드
**품질 (quality)**:
- [x] 심각도 (severity: 낮음/보통/높음)
- [x] 품목 (item: PP/PE - 선택)
- [x] 생산품 (kind: 압축품/분쇄품/펠렛 - 선택)
- [x] 상태 (q_status: 진행중/완료)
- [x] 원인 (q_cause), 조치 (q_action), 예방대책 (q_prevent)

**설비 (equipment)**:
- [x] 설비 선택 (e_equipmentId, e_equipmentLabel)
- [x] 심각도 (e_severity)
- [x] 상태 (e_status)
- [x] 증상 (e_symptom), 조치 (e_action), 예방대책 (e_prevent)

**안전 (safety)**:
- [x] 직원 선택 (s_employeeId, s_employeeLabel)
- [x] 위험도 (s_risk: 낮음/보통/높음)
- [x] 장소 (s_location), 조치 (s_action), 예방대책 (s_prevent)

#### 버튼
- [x] 카테고리 선택 버튼 (품질/설비/안전)
- [x] 저장 (onSubmit → addIssueItem)
- [x] 취소 (embedded 모드에서만)
- [x] 삭제 (목록 항목별)

#### 자동 기능
- [x] 제목 자동 생성 (useAutoTitle 훅)
  - 형식: `{recordDate} {writerName} {writerRole} [품질/설비/안전] 이슈기록`
- [x] 태그 추천 (useTagSuggestion 훅)
  - 2글자 이상 타이핑 시 추천
  - 시스템 태그 / 개인 태그 구분
  - 자동 커밋 (Enter, 띄어쓰기)
- [x] 품질/설비 완료 시 → 자동으로 조치 입력 폼 표시
  - ActionForm embedded 모드
  - 이슈와 조치 자동 연계

#### 저장 방식 (확정)
- **저장소**: localStorage → `issue_docs_v1`
- **구조**: 문서 ID 기반 항목 누적
  - ID: `ISSUE_{recordDate}_{writerName}`
  - 1개 문서 안에 items[] 배열
  - `addIssueItem` → 새 항목 추가 (배열 앞에 push)
- **API**: [company-docs/src/data/issueRepo.ts](company-docs/src/data/issueRepo.ts)
  - `addIssueItem(recordDate, writerName, item)`
  - `listIssueItems(recordDate, writerName)`
  - `updateIssueStatus(issueId, status, by, linkedActionId?)`

#### 목록 표시
- [x] 오늘 등록한 이슈 표시 (savedItems)
- [x] 제목, 상태 (진행중/완료), 카테고리
- [x] 상세 내용 미리보기 (50자)
- [x] 조치 연계 여부 표시 (linkedActionId)
- [x] 삭제 버튼

#### 삭제/수정 정책
- 항목 단위 삭제 가능 (removeIssueItem)
- 수정 불가 (재입력 방식)
- 상태 변경은 조치와 연계 시 자동 업데이트

---

### 1.3 Action (조치기록)
**페이지**: [company-docs/src/app/pages/register/RegisterAction.tsx](company-docs/src/app/pages/register/RegisterAction.tsx)  
**공통 폼**: [company-docs/src/base/components/form/ActionForm.tsx](company-docs/src/base/components/form/ActionForm.tsx)

#### 입력 필드
- [x] 기록일 (recordDate)
- [x] 작성자 이름 (writerName)
- [x] 작성자 직책 (writerRole)
- [x] 지부 (site: 대구/성주 - 선택)
- [x] 제목 (title - 자동 생성)
- [x] 상세 내용 (details)
- [x] 태그 (tagsText)
- [x] 이슈 연계 (issueId, issueLabel - 드롭다운)
- [x] 정비업체 (vendorId, vendorLabel - 드롭다운)
- [x] 정비 비용 (vendorCost)

#### 버튼
- [x] 저장 (onSubmit → addActionItem)
- [x] 취소 (embedded 모드에서만)
- [x] 삭제 (목록 항목별)

#### 자동 기능
- [x] 제목 자동 생성 (useAutoTitle 훅)
  - 형식: `{recordDate} {writerName} {writerRole} [품질/설비/안전] 조치기록`
  - embedded 모드에서는 이슈의 category 활용
- [x] 태그 추천 (useTagSuggestion 훅)
  - 동일한 타이핑 추천 로직
- [x] 이슈 연계 시 → 자동으로 이슈 상태를 "완료"로 변경
  - `updateIssueStatus(issueId, "완료", writerName, actionId)`

#### 저장 방식 (확정)
- **저장소**: localStorage → `local_action_docs_v1`
- **구조**: 문서 ID 기반 항목 누적
  - ID: `ACTION_{recordDate}_{writerName}`
  - 1개 문서 안에 items[] 배열
  - `addActionItem` → 새 항목 추가 (배열 push)
- **API**: [company-docs/src/data/actionRepo.ts](company-docs/src/data/actionRepo.ts)
  - `addActionItem(date, writer, item)`
  - `listActionItems(date, writer)`

#### 목록 표시
- [x] 오늘 등록한 조치 표시 (savedItems)
- [x] 제목, 상세 내용 미리보기 (50자)
- [x] 연계된 이슈 표시 (issueLabel)
- [x] 정비업체 표시 (vendorLabel)
- [x] 태그 표시 (최대 5개)
- [x] 삭제 버튼

#### 삭제/수정 정책
- 항목 단위 삭제 가능 (removeActionItem)
- 수정 불가 (재입력 방식)

---

## 2. 데이터 스키마 요약

### 2.1 Production
**파일**: [company-docs/src/domain/schema/daily/production.ts](company-docs/src/domain/schema/daily/production.ts)

#### ProductionDraft (입력 폼)
```
recordDate, writerName, writerRole, site,
title, details, tagsText,
lines: ProductionLine[]
```

#### ProductionLine
```
id, shift, product, item, bags, kg, memo
```

#### ProductionRecord (저장 문서)
```
id, kind: "production",
recordDate, createdAt, updatedAt,
writerName, writerRole, title, details, tags,
site, lines[]
```

#### 함수 (확정)
- `defaultProductionDraft()` ✅
- `normalizeProductionDraft(raw)` ✅
- `validateProductionDraft(draft)` ✅
- `toProductionRecord(draft, stableId?)` ✅
- `makeProductionDocId(recordDate, site, writerName)` ✅

---

### 2.2 Issue
**파일**: [company-docs/src/domain/schema/daily/issue.ts](company-docs/src/domain/schema/daily/issue.ts)

#### IssueDraft (입력 폼)
```
recordDate, writerName, site, tagsText,
category, title, details,

// 품질
q_severity, q_item, q_kind, q_status, q_cause, q_action, q_prevent,

// 설비
e_equipmentId, e_equipmentLabel, e_severity, e_symptom, e_action, e_prevent, e_status,

// 안전
s_employeeId, s_employeeLabel, s_risk, s_location, s_action, s_prevent
```

#### IssueItem (저장 항목)
```
id, recordDate, actualCreatedAt, site,
createdAt, updatedAt,
writerName, title, details,
category, categoryLabel, status, severity,
history: IssueHistoryEntry[],
linkedActionId,
quality?: IssueQuality,
equipment?: IssueEquipment,
safety?: IssueSafety,
equipmentId, equipmentLabel, employeeId, employeeLabel,
tags, tagIds
```

#### IssueDoc (저장 문서)
```
id: ISSUE_{recordDate}_{writerName},
recordDate, writerName,
items: IssueItem[],
updatedAt
```

#### 함수 (확정)
- `defaultIssueDraft()` ✅
- `normalizeIssueDraft(d)` ✅ (파일에서 확인 필요)
- `validateIssueDraft(d)` ✅ (파일에서 확인 필요)
- `toIssueItem(draft)` ✅
- `makeIssueDocId(recordDate, writerName)` ✅

---

### 2.3 Action
**파일**: [company-docs/src/domain/schema/daily/action.ts](company-docs/src/domain/schema/daily/action.ts)

#### ActionDraft (입력 폼)
```
recordDate, writerName, writerRole, site, tagsText,
title, details,
issueId, issueLabel,
vendorId, vendorLabel, vendorCost
```

#### ActionItem (저장 항목)
```
id, recordDate, actualCreatedAt, site,
createdAt, updatedAt,
writerName, writerRole, title, details,
issueId, issueLabel,
vendorId, vendorLabel, vendorCost,
tags, tagIds
```

#### ActionDoc (저장 문서)
```
id: ACTION_{recordDate}_{writerName},
recordDate, writerName,
items: ActionItem[],
updatedAt
```

#### 함수 (확정)
- `defaultActionDraft()` ✅
- `normalizeActionDraft(d)` ✅
- `validateActionDraft(d)` ✅
- `toActionItem(draft)` ✅

---

## 3. 연계 관계 (다이어그램)

```
┌─────────────────┐
│ Production      │
│ (생산기록)       │
└────────┬────────┘
         │
         │ (패널 내 이슈 추가)
         ├───────────────────────┐
         │                       │
         v                       v
┌─────────────────┐     ┌────────────────┐
│ Issue           │────▶│ Action         │
│ (이슈기록)       │     │ (조치기록)      │
└─────────────────┘     └────────────────┘
         │                       ▲
         │                       │
         └───────────────────────┘
           이슈 해결완료 시 자동 연계
           (linkedActionId, issueId)
```

### 연계 방식 (확정)

1. **Production → Issue**
   - 생산일지 작성 중 "이슈 추가" 패널에서 IssueForm 사용
   - 저장 시 `addIssueItem(issueDraft.recordDate, issueDraft.writerName, item)`
   - 연계 키: 없음 (독립 저장)

2. **Issue → Action**
   - **경로 1**: RegisterIssue에서 품질/설비 이슈를 "완료"로 저장 시
     - 자동으로 ActionForm embedded 모드 표시
     - `linkedIssue: { id, title }` 전달
     - 저장 시 `updateIssueStatus(issueId, "완료", writerName, actionId)`
   - **경로 2**: RegisterAction에서 진행중 이슈 선택
     - `pendingIssues` 목록에서 선택
     - `issueId`, `issueLabel` 저장
     - 저장 시 `updateIssueStatus(issueId, "완료", writerName, actionId)`

3. **연계 데이터 저장 위치**
   - IssueItem: `linkedActionId` 필드
   - ActionItem: `issueId`, `issueLabel` 필드
   - IssueItem.history: 연계 이력 기록 (`action_linked`)

---

## 4. 중복 로직 탐지 (정본 후보)

### 4.1 제목 자동 생성
**중복 발견**:
- Production: `buildAutoTitle(recordDate, writerName, writerRole)` → `{date} {name} {role} 생산일지`
- Issue: `useAutoTitle` 훅 → `{date} {name} {role} [category] 이슈기록`
- Action: `useAutoTitle` 훅 → `{date} {name} {role} [category] 조치기록`

**정본 위치**: [company-docs/src/base/hooks/useAutoTitle.ts](company-docs/src/base/hooks/useAutoTitle.ts) ✅ (이미 훅으로 정리됨)

**문제점**: Production에는 여전히 `buildAutoTitle` 함수가 별도로 존재

---

### 4.2 태그 추천 로직
**중복 발견**:
- Production: `typingSuggestions(token)` + `buildSuggestions()` (legacy)
- Issue/Action: `useTagSuggestion` 훅 → `getTypingSuggestions(token)`

**핵심 로직 (동일)**:
1. 2글자 이상 토큰 추출
2. prefix/contains/suffix 매칭
3. 3-gram 윈도우 보조
4. 점수 기반 정렬 (매칭 강도 → 태그 길이)
5. dismissed 집합 관리
6. 더보기 (30개 제한)

**정본 위치**: [company-docs/src/base/hooks/useTagSuggestion.ts](company-docs/src/base/hooks/useTagSuggestion.ts) ✅

**문제점**: Production에는 별도 함수로 중복 구현

---

### 4.3 태그 색상 결정 (시스템 vs 개인)
**중복 발견**:
- Production: 로컬에서 `systemTagSet` 빌드 + tone 함수
- Issue/Action: `useTagSuggestion` 내부에서 `getTone` 함수

**로직 (동일)**:
- 시스템 태그 판정: 기준정보(설비/직원/거래처 등) + 시스템 태그 인덱스
- 색상 구분: `tone === "system"` → accent, `tone === "personal"` → 기본

**정본 위치**: [company-docs/src/base/hooks/useTagSuggestion.ts](company-docs/src/base/hooks/useTagSuggestion.ts) 내부

---

### 4.4 저장 (upsert) 패턴
**중복 발견**:
- Production: `upsertSave()` → ID 생성 + toRecord + repo.upsert
- Issue: `addIssueItem()` → ID 생성 + 문서 find/update + saveAll
- Action: `addActionItem()` → ID 생성 + 문서 find/update + saveJson

**공통 패턴**:
1. stable ID 생성 (날짜 + 작성자 기반)
2. 기존 문서 찾기
3. 있으면 items 배열에 추가, 없으면 새 문서 생성
4. 전체 배열 다시 저장

**정본 후보**: 없음 (각 repo가 독립적으로 관리)

---

### 4.5 Draft 관리 (localStorage)
**중복 발견**:
- Production: `useDraftState` 훅 사용 → KEY_DRAFT
- Issue/Action: IssueForm/ActionForm 내부에서 useState로 관리

**차이점**:
- Production: Draft를 localStorage에 자동 저장 (복원 가능)
- Issue/Action: 폼 컴포넌트가 초기화되면 Draft 사라짐

---

## 5. 정본으로 빼야 하는 후보 (Top 5)

1. **제목 자동 생성 로직**
   - 현재: `useAutoTitle` 훅 (Issue/Action) vs `buildAutoTitle` 함수 (Production)
   - 목표: Production도 `useAutoTitle` 훅 사용

2. **태그 추천 로직**
   - 현재: `useTagSuggestion` 훅 (Issue/Action) vs `typingSuggestions` 함수 (Production)
   - 목표: Production도 `useTagSuggestion` 훅 사용

3. **태그 색상 결정 로직 (시스템 vs 개인)**
   - 현재: 각 페이지에서 systemTagSet 빌드
   - 목표: `useTagSuggestion` 훅에서 `getTone` 함수 export

4. **Draft 상태 관리**
   - 현재: Production은 `useDraftState`, Issue/Action은 useState
   - 목표: 공통 Draft 훅 (`useDraft`) 생성 (localStorage 자동 저장/복원)

5. **기준정보 후보 풀 빌드 (candidatePool)**
   - 현재: Production과 useTagSuggestion에서 각각 중복 빌드
   - 목표: 공통 유틸 함수로 추출 (`buildCandidatePool`)

---

## 6. 현재 구조의 위험 (유지보수 관점)

1. **태그 추천 로직 수정 시 3곳 동시 수정 필요**
   - Production의 `typingSuggestions`
   - useTagSuggestion의 `getTypingSuggestions`
   - (이슈 패널) Production 내부의 `issueTypingSuggestions`

2. **시스템 태그 판정 기준 변경 시 2곳 수정 필요**
   - Production: 로컬 systemTagSet 빌드
   - useTagSuggestion: buildSystemTagSet

3. **기준정보 종류 추가 시 4곳 수정 필요**
   - Production: candidatePool 빌드
   - useTagSuggestion: buildCandidatePool
   - useTagSuggestion: buildSystemTagSet
   - (이슈 패널) Production 내부 후보 빌드

4. **저장 패턴 변경 시 3개 repo 동시 수정 필요**
   - issueRepo: addIssueItem
   - actionRepo: addActionItem
   - localRepo: productionDaily upsert

5. **제목 형식 변경 시 2곳 수정 필요**
   - Production: buildAutoTitle
   - useAutoTitle 훅

---

## 7. 리팩터링 최소 플랜 (2단계)

### 단계 1: 정본화할 훅/블록 후보 + 제거 지점

#### 1.1 Production에서 useAutoTitle 도입
- [ ] [RegisterProductionDaily.tsx](company-docs/src/app/pages/register/RegisterProductionDaily.tsx)에서 `buildAutoTitle` 함수 제거
- [ ] `useAutoTitle` 훅 import 및 사용
- [ ] `titleAuto`, `titleTouchedRef` 상태 → 훅으로 위임

#### 1.2 Production에서 useTagSuggestion 도입
- [ ] `typingSuggestions` 함수 제거
- [ ] `buildSuggestions` 함수 제거 (legacy)
- [ ] `candidatePool` 로컬 빌드 제거
- [ ] `useTagSuggestion` 훅 import 및 사용
- [ ] `dismissed`, `showAll` 상태 → 훅으로 위임
- [ ] `getTone` 함수 → 훅에서 export 받기

#### 1.3 Production 이슈 패널에서도 useTagSuggestion 적용
- [ ] `issueTypingSuggestions` 함수 제거
- [ ] 이슈 패널도 `useTagSuggestion` 훅 사용 (scope: "production-issue")

#### 1.4 Draft 관리 통일
- [ ] 새 훅 생성: `useDraft<T>(key, defaultValue)`
  - localStorage 자동 저장/복원
  - normalize 함수 받기
- [ ] IssueForm, ActionForm에서도 적용 검토

#### 1.5 candidatePool 빌드 함수 추출
- [ ] 새 유틸 함수: `buildCandidatePool(): Sug[]`
  - 경로: [company-docs/src/base/utils/candidatePool.ts](company-docs/src/base/utils/candidatePool.ts) (추정)
  - 기준정보 전체 + 시스템/개인 태그
- [ ] useTagSuggestion에서 사용
- [ ] Production에서 제거

---

### 단계 2: 페이지는 조립만 하도록 변경

#### 2.1 페이지 역할 축소
- Production/Issue/Action 페이지는 다음만 담당:
  - 기본 정보 입력 (날짜/작성자/지부)
  - 공통 폼 컴포넌트 렌더링
  - 저장/삭제 이벤트 핸들링
  - 목록 표시

#### 2.2 비즈니스 로직 → 훅/유틸로 이동
- [ ] 제목 자동 생성 → `useAutoTitle`
- [ ] 태그 추천 → `useTagSuggestion`
- [ ] Draft 관리 → `useDraft`
- [ ] 저장 → repo의 `addItem` / `upsert`
- [ ] 연계 로직 → repo의 `updateIssueStatus`

#### 2.3 페이지 코드 구조 목표
```
페이지 컴포넌트:
1. 기본 상태 (날짜/작성자)
2. 훅 호출 (useAutoTitle, useTagSuggestion, useDraft)
3. 저장 핸들러 (repo.add 호출만)
4. 목록 조회 (repo.list 호출)
5. 렌더링 (폼 + 목록)

→ 페이지 내부에 로직 없음, 단순 조립만
```

#### 2.4 공통 폼 컴포넌트 역할
- IssueForm, ActionForm은 입력 UI만 담당
- 비즈니스 로직은 훅에 위임
- Props로 제어 (embedded, linkedIssue 등)

#### 2.5 제거할 것
- [ ] Production의 `buildAutoTitle` 함수
- [ ] Production의 `typingSuggestions`, `buildSuggestions` 함수
- [ ] Production의 `candidatePool`, `systemTagSet` 로컬 빌드
- [ ] 이슈 패널의 `issueTypingSuggestions` 함수
- [ ] 각 페이지의 태그 색상 판정 로직 (getTone → 훅에서 제공)

---

## 8. 추가 확인 필요 (추정 vs 확정)

### 확정 (코드 확인 완료)
- ✅ Production 저장: `repo.productionDaily().upsert()`
- ✅ Issue 저장: `addIssueItem(recordDate, writerName, item)`
- ✅ Action 저장: `addActionItem(date, writer, item)`
- ✅ 제목 자동: `useAutoTitle` 훅 존재
- ✅ 태그 추천: `useTagSuggestion` 훅 존재
- ✅ Issue-Action 연계: `updateIssueStatus()` + `linkedActionId`

### 추정 (파일 부분 확인)
- 🔍 `normalizeIssueDraft`, `validateIssueDraft` 함수 존재 여부
  - 파일: [company-docs/src/domain/schema/daily/issue.ts](company-docs/src/domain/schema/daily/issue.ts) (150줄까지만 확인)
- 🔍 Production의 이슈 패널 저장 로직 상세
  - 추정: `addIssueItem` 호출 (확인됨 - line 785)

---

## 9. 참고: 파일 경로 요약

### 페이지
- Production: [company-docs/src/app/pages/register/RegisterProductionDaily.tsx](company-docs/src/app/pages/register/RegisterProductionDaily.tsx)
- Issue: [company-docs/src/app/pages/register/RegisterIssue.tsx](company-docs/src/app/pages/register/RegisterIssue.tsx)
- Action: [company-docs/src/app/pages/register/RegisterAction.tsx](company-docs/src/app/pages/register/RegisterAction.tsx)

### 공통 폼
- IssueForm: [company-docs/src/base/components/form/IssueForm.tsx](company-docs/src/base/components/form/IssueForm.tsx)
- ActionForm: [company-docs/src/base/components/form/ActionForm.tsx](company-docs/src/base/components/form/ActionForm.tsx)

### 스키마
- Production: [company-docs/src/domain/schema/daily/production.ts](company-docs/src/domain/schema/daily/production.ts)
- Issue: [company-docs/src/domain/schema/daily/issue.ts](company-docs/src/domain/schema/daily/issue.ts)
- Action: [company-docs/src/domain/schema/daily/action.ts](company-docs/src/domain/schema/daily/action.ts)

### 저장소
- issueRepo: [company-docs/src/data/issueRepo.ts](company-docs/src/data/issueRepo.ts)
- actionRepo: [company-docs/src/data/actionRepo.ts](company-docs/src/data/actionRepo.ts)
- localRepo: [company-docs/src/data/localRepo.ts](company-docs/src/data/localRepo.ts)

### 훅
- useAutoTitle: [company-docs/src/base/hooks/useAutoTitle.ts](company-docs/src/base/hooks/useAutoTitle.ts)
- useTagSuggestion: [company-docs/src/base/hooks/useTagSuggestion.ts](company-docs/src/base/hooks/useTagSuggestion.ts)

---

## 10. 결론

세 페이지(Production/Issue/Action)는 **기능적으로 유사한 패턴**을 공유하지만, **구현 방식이 분산**되어 있어 유지보수 비용이 높음.

**핵심 중복**:
1. 제목 자동 생성 (2개 구현)
2. 태그 추천 로직 (3개 구현)
3. 태그 색상 판정 (2개 구현)
4. Draft 관리 (2개 방식)
5. candidatePool 빌드 (2개 구현)

**리팩터링 효과**:
- 수정 지점 감소: 태그 추천 로직 변경 시 3곳 → 1곳
- 일관성 향상: 모든 페이지에서 동일한 UX
- 테스트 용이: 훅 단위 테스트 가능
- 신규 페이지 추가 시 빠른 개발 (훅 재사용)
