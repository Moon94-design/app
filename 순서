Registry 공용 스키마/로직 먼저
normalizeName(), scoreMatches(), rename(), create() 같은 것
NamePicker 컴포넌트 만들기
입력 + 드롭다운 + 수정( rename ) + create
이걸 관계기관(RegisterAgency)부터 적용
그 다음에 사무기록에서 “관계기관 선택”은 무조건 NamePicker로 통일


옵션 1) 기존 페이지를 스키마로 교체 (추천)
사무기록(RegisterOfficeDaily) → domain/schema/daily/office.ts 연결
recordDate/createdAt 정책 확정
tagsText → tags[]는 기존 태그 입력 UX 그대로 사용
생산일지 → production 스키마 연결
유통 → logistics
설비 → equipment
재고 → inventory
품질 → quality
안전 → safety
회계 → accounting


옵션 2) ‘명칭 선택+추가등록’ 공용화 확대
지금 관계기관에서 중복 방지/수정/드롭다운이 잘 됐으니, 같은 패턴을
거래처
설비
차량
품목/재고 품목 쪽으로 확장하면 “연계 강제” 기반이 더 단단해짐.