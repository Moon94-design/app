# SECURITY_CHECKLIST.md
작성일: 2026-02-10
목적: src2 전환 과정에서 보안/안전 리스크를 빠르게 점검하고, 서버 이관(Phase 6) 전에 선조치 항목을 고정한다.

================================================================================
S0) 즉시 점검 (매 작업/PR 공통)
- [ ] `npm run check:security` 통과
- [ ] `src2/kernel/**`에서 `@legacy` import 0
- [ ] `src2/app/pages/**`에서 `repo/impl/*` 직접 import 0
- [ ] `src2`에서 `localStorage` 직접 접근 0 (예외: `kernel/repo/storage/*`)
- [ ] `eval/new Function` 사용 0

================================================================================
S1) 클라이언트 데이터 안전
- [ ] Storage key는 `src2/kernel/repo/keys.ts`만 사용 (하드코딩 0)
- [ ] draft key는 `draft:` prefix 규칙 준수
- [ ] 레거시 호환 마이그레이션(예: local_* -> repo:*)은 1회성 메타 플래그로 재실행 방지
- [ ] 부분 이관 데이터 병합 시 id dedupe 보장

================================================================================
S2) 라우팅/접근 제어(클라이언트 단계)
- [ ] deep route 새로고침(F5) 404 0 (`npm run test:smoke` 기준)
- [ ] 민감 화면 진입 경로가 하드코딩된 우회 링크로 노출되지 않는지 점검
- [ ] 에러 바운더리에서 스택/내부 경로 과다 노출 방지(사용자 메시지 최소화)

================================================================================
S3) 서버 이관 전 준비(Phase 6 선조치)
- [ ] 권한 매트릭스 초안 작성 (admin/editor/viewer + endpoint)
- [ ] 감사 로그 최소 필드 정의 (사용자, 액션, 대상, 시각)
- [ ] 입력 검증 기준 초안 확정 (DTO/schema)
- [ ] 파일 업로드 정책 초안 확정 (확장자, 크기, 중복, 부분 실패)

================================================================================
S4) 운영 룰
- [ ] 보안 관련 예외 허용 시 `DECISIONS_LOG.md`에 반드시 기록
- [ ] 체크리스트 미통과 상태로 다음 Phase 구현 착수 금지
