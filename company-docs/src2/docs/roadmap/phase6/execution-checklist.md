# Phase 6 실행 체크리스트 (REST + Read-First)

작성일: 2026-02-09
목적: Phase 6를 터지지 않게 순서 고정(Partner 기준 read-first 전환)

================================================================================
사전 고정값
- API 스타일: REST 고정
- 대상 도메인(1차): Partner
- 전환 순서: Read -> Write -> local fallback 제거
- DTO 정책: `kernel/schema` 기반 파생 또는 `kernel` 단일 매핑 SSOT

================================================================================
0) 시작 전 점검
- [ ] `DOCS_GUIDE.md`, `main_rule.md`, `MIGRATION_STATUS.md` 최신 상태 확인
- [ ] Phase 5 안정화 확인(핵심 흐름 깨짐 없음)
- [ ] 민감 파일(.env/개인 폴더) 수정 계획 없음

================================================================================
1) REST 계약 고정 (Partner)
- [ ] `GET /api/v1/partners` (목록)
- [ ] `GET /api/v1/partners/{id}` (단건)
- [ ] `POST/PUT` 및 bulk endpoint는 read 전환 완료 후 확정
- [ ] 에러 포맷 표준화(code/message/details/requestId)
- [ ] DTO <-> schema 매핑 표 작성(불일치 0)

================================================================================
2) 인증/권한 최소 정책
- [ ] 인증 방식 고정(세션/토큰 중 1개)
- [ ] 최소 권한 매트릭스 작성(admin/editor/viewer)
- [ ] Partner read endpoint 접근 권한 명시

================================================================================
3) DB/마이그레이션 최소 세트
- [ ] Partner 테이블/인덱스 초안 확정
- [ ] migration 1차(DDL + 최소 seed) 작성
- [ ] local -> server 필드 매핑 문서화

================================================================================
4) Read-First 전환 (필수 순서)
- [ ] domain repo에서 Partner `getAll/getById`만 server impl로 전환
- [ ] `upsert/upsertMany/remove`는 기존(local/fallback) 유지
- [ ] 기능 플래그로 read 전환 on/off 가능
- [ ] `npm run build` 통과
- [ ] `/manage/master` read 동작 수동 검증(목록/상세/새로고침)

================================================================================
5) Write 전환 (Read 안정화 후)
- [ ] `upsert/upsertMany/remove` server impl 전환
- [ ] 배치 쓰기(`upsertMany`) 실패 시 응답/롤백 정책 명시
- [ ] `npm run build` 통과
- [ ] 등록/수정/일괄수정 E2E 검증

================================================================================
6) local fallback 제거
- [ ] read/write 모두 server 경로 안정화 확인
- [ ] fallback 제거 후 회귀 테스트(목록/상세/등록/수정/일괄)
- [ ] 장애 시 임시 롤백 플랜 문서화

================================================================================
7) 업로드/엑셀 정책(Phase 6 필수)
- [ ] idempotency 키 정책 확정(동일 파일/동일 배치 중복 방지)
- [ ] 부분 실패 전략 확정(전체 실패 vs 부분 반영)
- [ ] 사용자 피드백 포맷 고정(오류 행 번호/사유/재시도 가이드)

================================================================================
8) 완료 기준 (Gate)
- [ ] Partner read-first 순서 준수 증빙(read -> write -> fallback 제거)
- [ ] DTO/schema/DB 매핑 불일치 0
- [ ] build 성공 + 핵심 경로 수동검증 완료
- [ ] result 문서 업데이트 완료
